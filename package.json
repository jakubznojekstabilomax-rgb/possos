{
  "name": "poss-os",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dist": "electron-builder --win --x64"
  },
  "build": {
    "appId": "com.poss-os-pos",
    "productName": "POSS OS",
    "win": {
      "target": "portable"
    },
    "icon": "build/favicon.ico",
    "artifactName": "${productName} Installer.${ext}"
  },
  "devDependencies": {
    "electron": "^28.2.3",
    "electron-builder": "^24.9.1"
  }
}
